# 交接：把檢測產品推薦移進「各癌種風險因子參考」

**狀態**：待實作。原本的獨立區塊已於 2026-09-22 從信件移除（`18bb062` 之後）。商店端的轉址**已部署且運作中**，不需要重做。

**要做的事**：在免費信的「各癌種風險因子參考」區塊裡，為每一個**中度風險以上且有對應產品**的癌別，在它的建議篩檢旁邊加上 OkaiDx 產品連結。

---

## 1. 已經做好、不要重做的部分

### 商店端轉址（已上線）

`contracts/purchase/egbio-access-code.php` 第 14 節。實際部署在 WordPress（`eg-bio-mdi-Sandbox`）。

```
https://mdi.eg-bio.com/recommend?c=<癌別>&src=<來源>
```

查表轉址並附上 utm 參數。已驗證的行為：

| `c=` | 落點 |
|---|---|
| 肝癌 | Gastrointestinal |
| 大腸直腸癌 | Colorectal |
| 胰臟癌 | Pancreatic |
| 乳癌 | Breast Monitoring |
| 其他任何值 | 產品總覽頁 `/product-category/test/` |

**信件端只要組出這個網址就好，不需要知道任何產品網址。** 這是刻意的：換產品不必編輯 Power Automate，而編輯流程是這套系統裡最危險的操作。

轉址回應帶 `nocache_headers()`，且 W3 Total Cache 的「Never cache」已加入 `/recommend*`。**不要拿掉任何一層** —— 2026-09-21 曾因快取把部署前的 404 存下來，部署後同一個網址繼續回 404，其他癌別卻正常。

### 癌別名稱的唯一事實來源

模型 `cancer_risks[].cancer` 回傳的字串，**不是**問卷的癌別選項清單。兩者不同：模型會回「膽道癌」，問卷選項裡沒有；問卷有「攝護腺癌／膀胱癌／腎癌」，觀察到的模型輸出裡沒出現過。

實際觀察到 9 種：大腸直腸癌、頭頸癌、乳癌、肺癌、子宮內膜癌、肝癌、胰臟癌、膽道癌、胃癌。產品叫「十癌」，所以這**不是完整清單** —— 任何對應表都必須有 fallback。

要重新確認時跑（資料 API 那台的 `/home/site/wwwroot/`）：

```bash
node list-cancer-labels.js 1000
```

---

## 2. 已定案的決策，不要重新討論

這些都問過 Abbie 或查過產品頁，附上依據。

### 2.1 只有四種癌別有產品

| 癌別 | 產品 | 依據（產品頁原文） |
|---|---|---|
| 大腸直腸癌 | Colorectal | — |
| 胰臟癌 | Pancreatic | — |
| 肝癌 | **Gastrointestinal** | 該產品涵蓋 pancreatic / colorectal / **liver**；沒有單項肝癌檢測 |
| 乳癌 | Breast Monitoring | "Intended for **monitoring the disease status** of breast cancer patients" |

**胃癌沒有產品。** Gastrointestinal 這個名字不含胃癌，產品頁明列涵蓋範圍只有上述三種。照名字把胃癌對上去，會把胃癌風險偏高的人導去一個不驗胃癌的檢測。

頭頸癌、肺癌、子宮內膜癌、膽道癌也沒有產品。**沒有產品的癌別不要放連結** —— 落到總覽頁的體驗比沒有連結更差。

### 2.2 乳癌只依病史，不依風險（Abbie 2026-09-22 確認「維持現況」）

乳癌那個產品是**監測已確診患者的疾病狀態**，不是篩檢。推給一個只是乳癌風險因子偏高的健康人，是錯誤的臨床用途。

- 乳癌 → 只有在 `personal_cancer_types` 含「乳癌」時才顯示
- 其他三種 → 依風險顯示（見 2.3）

### 2.3 依風險顯示的條件

該癌別在 `cancer_risks` 裡自己那一筆必須：

- `level` 為 `中度風險` 或 `高風險`
- `reliable` 為 `true` —— 探索性結果會排在前面但不可靠，拿它推銷檢測是這個功能最糟的失誤
- 該癌別**不在** `personal_cancer_types` 裡 —— Colorectal 與 Pancreatic 都是 **detection**（偵測）產品，推給已確診者，與把監測產品推給健康人是同一種錯誤的兩個方向

### 2.4 文案受產品頁約束

乳癌頁寫了監測用途，文案才可以那樣講。另外三個產品頁目前只寫 "early health screening purposes"，**不可以**替它們宣稱追蹤或監測用途 —— 讀者點進去會看到一個和信件說法不符的頁面。

那三頁補上追蹤用途之後才可以改文案。

### 2.5 未解決：讀者可能買不到

這些是美國 CLIA 實驗室的 LDT、USD 計價、**需要醫師授權**、要寄採集套組到府抽血。免費線的讀者主要在台灣。

問卷有收 `country`（`excel_row.country`，中文字串如「臺灣」「中國」），所以連結可以只對特定國家顯示。**Abbie 尚未決定**，目前沒有做。

---

## 3. 技術上的難處：那段排序是一個不透明字串

「各癌種風險因子參考」整段是這樣渲染的：

```html
<div style="white-space:pre-line;...">@{body('HTTP_AI_predict')['cancer_risks_text']}</div>
```

`cancer_risks_text` 是模型預先排版好的**單一字串**（含換行），而「建議篩檢」只存在於這個字串裡面。`cancer_risks` 陣列的元素只有 `{cancer, factors, level, level_display, n, pct, ratio, reliable}` —— **沒有篩檢建議**。

所以有兩條路：

### 做法 A：字串取代

用 `replace()` 在 `cancer_risks_text` 裡把癌別名稱換成「名稱＋連結」。

- 優點：不動流程結構，只改信件本文
- 難處：需要判斷「這個癌別是否中度以上」，而 Logic Apps 運算式**沒有 lambda**，無法在陣列裡依值查找元素。可能要靠字串本身是否含等級文字來判斷，那很脆弱
- **必要的前置作業**：先看清楚 `cancer_risks_text` 的實際格式。腳本已備好：`egbiomed-ai-data-api/show-cancer-text.js`，上傳到 `/home/site/wwwroot/` 後 `node show-cancer-text.js`

### 做法 B：Filter array + Select 兩個動作

1. **篩選陣列**：從 `cancer_risks` 取出符合條件的元素
2. **選取**：把每個元素映射成一段 HTML
3. 信件用 `join(...)` 輸出

- 優點：條件判斷乾淨，不必解析字串
- 缺點：要在流程 B 加兩個動作；而且產生的是**獨立清單**，不是插在每個癌別的建議篩檢旁邊 —— 與 Abbie 要的位置不符

**建議**：先跑 `show-cancer-text.js` 看格式再決定。格式若每個癌別自成一段且含等級文字，做法 A 可行；否則可能需要請模型 API 端把篩檢建議也放進 `cancer_risks` 陣列，那是最乾淨的解法。

---

## 4. 被移除的實作，可以參考

`18bb062~1` 的 `scripts/build-email-templates.js` 有完整的可運作版本：條件運算式、中英文案、產品清單、以及展開巢狀 `if` 的產生器程式碼。對應的測試在同一個 commit 的 `test-free-email.js`。

```bash
git show 18bb062~1:scripts/build-email-templates.js
```

那版是「整封信最多一顆按鈕、放在付款區塊下方」。條件邏輯（2.2 / 2.3）可以直接沿用，要換的是**放在哪裡**與**每個癌別各一條**。

---

## 5. 不要弄壞的東西

- **流程 A 不要動。** 它服務付費的機構客戶。免費信是從付費樣板產生的，所以改的是 `scripts/build-email-templates.js`，不是手改 HTML
- **「免費信 = 付費信 + 目錄 + 付款區塊」那個測試**（`test-free-email.js`）會擋住任何讓兩者內容分岔的改動。新增區塊時要更新它移除區塊的邏輯，不要放寬比對
- **信件樣板不要手改。** 產生器重新產出會蓋掉。改產生器
- **貼進流程之後一定要驗證行為，不要只看有沒有存檔成功。** 條件式讀到不存在的欄位會得到 false 而不是錯誤 —— 流程不會失敗、不會有紀錄、信照常寄出

---

## 6. 怎麼驗證

不要只測「會顯示」那一側。2026-09-21 的一次測試顯示了按鈕，但那顆是風險分支產生的，病史分支其實壞著 —— 兩者外觀相同，只有文案不同。

| 測試情境 | 預期 |
|---|---|
| 病史勾乳癌 | 乳癌連結出現，文案是「您在問卷中表示曾被診斷為…」 |
| 病史勾大腸直腸癌，且模型第一名也是大腸直腸癌 | 出現的是**病史**文案，不是風險文案 |
| 無病史，某癌別中度以上且有產品 | 該癌別出現風險文案的連結 |
| 無病史，胃癌中度以上 | **沒有連結** |
| 某癌別低風險 | **沒有連結** |

用 `egbiotest2026` 送件測試。工作階段預設 30 分鐘（`ACCESS_GATE_SESSION_TTL_HOURS`），填太久會在送出時被閘門擋掉，答案會全部消失 —— 測之前建議先調成 `2`。

查不出「資料到底有沒有送進流程」時，**在流程裡加一個暫時的「編輯」動作**（zh-TW 介面把 Compose 譯作「編輯」，在「資料作業」分類下），輸出 `string(triggerBody()?['excel_row'])`，跑一次再看執行紀錄。2026-09-22 就是靠這個找出 `personal_cancer_types` 一直是空字串的，在那之前有四次從程式碼推論的判斷全是錯的。
